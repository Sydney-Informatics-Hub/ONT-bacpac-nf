nextflow_process {

    name "Test Process unzip_fastqs"
    script "modules/unzip_fastqs.nf"
    process "unzip_fastqs"

    test("Should run without failures") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                input[0] = [ "example_sample", file("${projectDir}/tests/data/example_sample.zip") ]
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

}
