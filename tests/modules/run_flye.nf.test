nextflow_process {

    name "Test Process flye_assembly"
    script "modules/run_flye.nf"
    process "flye_assembly"

    test("Should run without failures") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                input[0] = channel.from([[ "example_sample", null, file("${projectDir}/tests/data/subset2000.fq.gz") ]])
                """
            }
        }

        then {
            assert process.success
            assert file(process.out.flye_assembly[0][2]).exists()
            assert file(process.out.flye_graph[0][2]).exists()
            def fasta_file = file(process.out.flye_assembly[0][2] + '/assembly.fasta')
            assert fasta_file.exists()
        }

    }

}
