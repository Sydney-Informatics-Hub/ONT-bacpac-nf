nextflow_process {

    name "Test Process abricateVFDB_annotation_chromosomes"
    script "modules/run_abricateVFDB_annotation_chromosomes.nf"
    process "abricateVFDB_annotation_chromosomes"

    test("Should run without failures") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                input[0] = channel.from([[ "example_sample", "flye", file("${projectDir}/tests/data/flye/assembly.fasta") ]])
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

}
